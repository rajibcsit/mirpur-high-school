@extends('layouts.app') @section('title','Latest News') @section('content')
<section class="bg-gradient-to-br from-[#072e1c] to-primary text-white py-20"><div class="max-w-7xl mx-auto px-4"><span class="hero-kicker">SCHOOL UPDATES</span><h1 class="text-5xl font-black mt-4">Latest News</h1><p class="text-white/70 mt-3">Stories, achievements, announcements and updates from our school.</p></div></section>
<section class="max-w-7xl mx-auto px-4 py-16"><div class="grid md:grid-cols-2 lg:grid-cols-3 gap-7">@forelse($news as $item)<a href="{{ route('news.show',$item->slug) }}" class="bg-white rounded-3xl overflow-hidden border border-gray-100 shadow-sm hover:shadow-xl transition hover:-translate-y-1">@if($item->image_path)<img src="{{ asset('storage/'.$item->image_path) }}" class="w-full h-56 object-cover" alt="{{ $item->title }}">@else<div class="w-full h-56 bg-gradient-to-br from-primary to-primary-dark grid place-items-center text-5xl">📰</div>@endif<div class="p-6"><div class="flex justify-between text-xs uppercase tracking-widest font-bold"><span class="text-gold">{{ $item->category ?: 'School News' }}</span><span class="text-gray-400">{{ $item->published_at?->format('d M Y') }}</span></div><h2 class="font-black text-xl text-gray-900 mt-3">{{ $item->title }}</h2><p class="text-gray-500 mt-2 line-clamp-3">{{ $item->excerpt }}</p><span class="text-primary font-bold inline-block mt-5">Read story →</span></div></a>@empty<p class="text-gray-500">No news published yet.</p>@endforelse</div><div class="mt-10">{{ $news->links() }}</div></section>@endsection
