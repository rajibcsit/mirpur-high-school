@extends('layouts.app') @section('title',$item->title) @section('content')
<section class="bg-gradient-to-br from-[#072e1c] to-primary text-white py-16"><div class="max-w-4xl mx-auto px-4"><span class="hero-kicker">{{ $item->category ?: 'SCHOOL NEWS' }}</span><h1 class="text-4xl md:text-6xl font-black mt-5">{{ $item->title }}</h1><p class="text-white/60 mt-4">{{ $item->published_at?->format('d F Y') }}</p></div></section>
<article class="max-w-4xl mx-auto px-4 py-14">@if($item->image_path)<img src="{{ asset('storage/'.$item->image_path) }}" class="w-full max-h-[520px] object-cover rounded-3xl mb-10" alt="{{ $item->title }}">@endif @if($item->excerpt)<p class="text-xl font-semibold text-gray-600 leading-8 mb-8">{{ $item->excerpt }}</p>@endif<div class="prose prose-lg max-w-none text-gray-700 whitespace-pre-line">{{ $item->content }}</div>@if($item->external_url)<a href="{{ $item->external_url }}" target="_blank" rel="noopener" class="btn-primary mt-8">Read External Link →</a>@endif</article>@endsection
