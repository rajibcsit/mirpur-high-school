@extends('layouts.admin') @section('title','Latest News') @section('page-title','Latest News') @section('content')
<div class="flex justify-end mb-6"><a href="{{ route('admin.news.create') }}" class="bg-primary text-white px-5 py-2.5 rounded-xl font-semibold">+ Add News</a></div><div class="bg-white rounded-2xl shadow-sm border overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-gray-50"><tr><th class="text-left p-4">News</th><th class="p-4">Category</th><th class="p-4">Status</th><th class="p-4">Published</th><th class="p-4">Action</th></tr></thead><tbody>@forelse($news as $item)<tr class="border-t hover:bg-gray-50"><td class="p-4"><b>{{ $item->title }}</b><div class="text-xs text-gray-500">{{ $item->slug }}</div></td><td class="p-4 text-center">{{ $item->category ?: '—' }}</td><td class="p-4 text-center"><span class="px-2.5 py-1 rounded-full text-xs {{ $item->is_published?'bg-green-100 text-green-700':'bg-gray-100 text-gray-600' }}">{{ $item->is_published?'Published':'Draft' }}</span>@if($item->is_featured)<span class="ml-1 px-2.5 py-1 rounded-full text-xs bg-amber-100 text-amber-700">Featured</span>@endif</td><td class="p-4 text-center">{{ $item->published_at?->format('d M Y') ?: '—' }}</td><td class="p-4 text-center"><a class="text-primary mr-3 font-semibold" href="{{ route('admin.news.edit',$item) }}">Edit</a><form class="inline" method="POST" action="{{ route('admin.news.destroy',$item) }}" onsubmit="return confirm('Delete this news?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-gray-500">No news yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $news->links() }}</div>@endsection
