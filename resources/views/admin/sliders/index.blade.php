@extends('layouts.admin')
@section('title','Hero Sliders') @section('page-title','Hero Sliders')
@section('content')
<div class="flex justify-between items-center mb-6"><div><p class="text-sm text-gray-500">Manage homepage hero slides.</p></div><a href="{{ route('admin.sliders.create') }}" class="bg-primary text-white px-5 py-2 rounded-lg">+ Add Slider</a></div>
<div class="bg-white rounded-xl shadow overflow-hidden"><table class="w-full text-sm"><thead class="bg-gray-50"><tr><th class="text-left p-4">Slide</th><th class="p-4">Order</th><th class="p-4">Status</th><th class="p-4">Actions</th></tr></thead><tbody>
@forelse($sliders as $s)<tr class="border-t"><td class="p-4 flex gap-4 items-center">@if($s->image_path)<img src="{{ asset('storage/'.$s->image_path) }}" class="w-28 h-16 rounded object-cover">@endif<div><div class="font-semibold">{{ $s->title }}</div><div class="text-xs text-gray-500">{{ $s->subtitle }}</div></div></td><td class="p-4 text-center">{{ $s->display_order }}</td><td class="p-4 text-center"><span class="px-2 py-1 rounded text-xs {{ $s->is_active?'bg-green-100 text-green-700':'bg-gray-100 text-gray-600' }}">{{ $s->is_active?'Active':'Hidden' }}</span></td><td class="p-4 text-center"><a href="{{ route('admin.sliders.edit',$s) }}" class="text-primary font-semibold mr-3">Edit</a><form class="inline" method="POST" action="{{ route('admin.sliders.destroy',$s) }}" onsubmit="return confirm('Delete this slide?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-gray-500">No sliders yet.</td></tr>@endforelse
</tbody></table></div><div class="mt-5">{{ $sliders->links() }}</div>
@endsection
